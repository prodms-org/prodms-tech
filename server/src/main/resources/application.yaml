server:
  port: ${SERVER_PORT:8087}

db:
  connection:
    string: ${DB_CONNECTION_STRING:jdbc:postgresql://localhost:5429/tech?password=pg-pwd&user=pg-user}


defaults:
  equipmentList:
    itemsPerPage: ${DEFAULTS_EQUIPMENT_LIST_ITEMS_PER_PAGE:10}
    page: ${DEFAULTS_EQUIPMENT_LIST_PAGE:0}
    sortCode: ${DEFAULTS_EQUIPMENT_SORT_CODE:0}
  equipmentSetList:
    itemsPerPage: ${DEFAULTS_EQUIPMENT_SET_LIST_ITEMS_PER_PAGE:10}
    page: ${DEFAULTS_EQUIPMENT_SET_LIST_PAGE:0}
    sortCode: ${DEFAULTS_EQUIPMENT_SET_SORT_CODE:0}
  blankList:
    itemsPerPage: ${DEFAULTS_BLANK_LIST_ITEMS_PER_PAGE:10}
    page: ${DEFAULTS_BLANK_LIST_PAGE:0}
    sortCode: ${DEFAULTS_BLANK_SORT_CODE:0}
